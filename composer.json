{
    "name": "dazzet/convertloop-wordpress",
    "description": "Convertloop for Wordpress",
    "type": "wordpress-plugin",
    "authors": [
        {
            "name": "Dazzet",
            "email": "info@dazzet.co"
        }
    ],
    "require": {
        "convertloop/convertloop-php": "^0.1.0"
    },
    "autoload":{
      "psr-4":{
        "WpConvertloop\\":"lib/"
      }
    },
    "scripts": {
      "zip": "T_DIR=`basename ${PWD}` && cd .. && zip -r ${T_DIR}/${T_DIR}.zip ${T_DIR} -x *.git* -x ${T_DIR}/README.md -x ${T_DIR}/composer.* -x ${T_DIR}/*.zip"
    }
}
